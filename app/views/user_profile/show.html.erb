<h3>
  <%= @profile.nickname %>
  <small> - <%= @profile.username %></small>
</h3>

<%= image_tag @profile.avatar %>

<div><%= simple_format h @profile.bio %></div>

<% if @profile == current_user.profile %>
  <%= link_to "編集する", user_profile_edit_path %>
<% end %>
